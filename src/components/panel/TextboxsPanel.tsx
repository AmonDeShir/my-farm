import styled from '@emotion/styled'
import Panel from "./panel";

const TextboxsPanel = styled(Panel)`
  display: flex;
  flex-direction: column;
  justify-content: space-around;
  align-items: center;
  padding: 5px 1px 5px 0;
`;

export default TextboxsPanel;